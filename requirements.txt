# 多源勘察数据融合系统 —— Python 依赖
#
# 安装：pip install -r requirements.txt
#
# 离线兜底模式（use_db=False）仅需"既有"部分。
# 完整模式（PostgreSQL/PostGIS）还需"新增"部分。

# ===== 既有（必装）=====
fastapi>=0.110
uvicorn[standard]>=0.27
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
python-docx>=1.1
pillow>=10.0
matplotlib>=3.7

# ===== 新增（分层架构 + PostgreSQL）=====
SQLAlchemy>=2.0
Alembic>=1.13
psycopg[binary]>=3.1
pydantic-settings>=2.1
loguru>=0.7

# PostGIS 几何列（仅 use_db=True 必装）
geoalchemy2>=0.14

# ===== 测试（可选）=====
pytest>=8.0
httpx>=0.27
