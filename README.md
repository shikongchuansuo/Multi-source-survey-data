# 多源勘察数据联动展示与证据链追溯系统

> **空天地多源勘察数据融合展示与风险证据链决策平台**
>
> 把 **正射影像、三维点云、物探剖面、钻孔柱状图、勘察报告** 统一挂到同一个工程位置上：
> 看得见数据 → 找得到位置 → 连得上证据 → 解释得清风险 → 给得出建议。

---

## ✨ 核心能力

| 能力 | 说明 |
|---|---|
| 🗺️ **多源空间联动** | 正射影像底图 + 线路轴线 + 钻孔点 + 物探测线 + 风险区，统一在工程坐标系下 |
| 🏔️ **三维点云/地形** | 基于 DEM 生成的坡度着色点云，选中风险区自动飞行定位并高亮边界 |
| 🌐 **多元数据三维融合** | **遥感影像地形 + 点云 + 隧道管体 + 钻孔分层柱 + 异常体椭球**，4 源数据同一 3D 场景融合展示，支持图层开关 |
| 🧭 **3D 空间感与坐标系** | 地面网格 + 带标签三轴(东/北/高程) + 高程刻度尺 + 指北针 + 比例尺 + 区域边界框 + 雾效深度层次 |
| 🔗 **证据链追溯** | 每个风险对象挂载 5 类证据（影像/点云/物探/钻孔/报告），可逐条追溯 |
| 💬 **智能对话引擎** | 自然语言交互：定位/查询/对比/解释/报告，多轮上下文记忆，对话直接驱动地图联动（**完全离线，无需 API**） |
| 🔍 **条件查询** | "坡度大于 30 度的风险""水位浅于 3 米的钻孔""K12+300 到 K12+800 之间的风险" |
| 📑 **多格式报告生成** | **Word(.docx)/Markdown/HTML** 三格式 × 单风险/全线综合两范围，Word 内嵌物探剖面与钻孔柱状图 |
| 📊 **多维证据可视化** | ECharts 风险雷达图 + 多风险对比叠加 + 钻孔地层条形图 + **物探电阻率交互式热力图** |
| 📐 **沿线地质纵剖面** | 地表高程 + 隧道设计高程 + 埋深 + 钻孔投影 + 风险区段标注，统一在里程轴上 |
| 📏 **里程轴联动** | K12+000~K13+000 里程轴，风险按位置落点，点击即定位 |

## 🎯 系统定位（不是普通可视化大屏）

普通展示只回答"**有什么数据**"。本系统回答"**这个风险为什么成立**"：

- **坐标**解决"在哪里"（影像/点云/钻孔落在同一空间）
- **里程**解决"工程上怎么描述"（K12+380 左侧边坡上方）
- **对象**解决"它是什么问题"（洞口边坡失稳 / 富水破碎带）
- **证据表**把五类数据串成一条可追溯的链

---

## 🚀 快速开始

### 一键运行（Windows）

双击 **`run.bat`** 即可。脚本会自动：
1. 检查并安装 Python 依赖（首次约 30 秒）
2. 生成多源勘察样例数据（首次约 10 秒）
3. 启动 Web 服务并自动打开浏览器

> 浏览器访问：**http://localhost:8000**

### 手动运行

```bash
# 1. 安装依赖
pip install fastapi uvicorn numpy pandas matplotlib pillow

# 2. 生成数据（仅首次）
python backend/generate_data.py

# 3. 启动服务
python -m uvicorn backend.app:app --host 0.0.0.0 --port 8000

# 4. 浏览器打开 http://localhost:8000
```

### 环境要求

- **Python 3.8+**（推荐 3.9 / 3.10）
- 浏览器：Chrome / Edge（需支持 WebGL）
- 无需联网 —— 前端三方库（Leaflet / Three.js / ECharts）已本地打包

---

## 📂 项目结构

```
Multivariate Data Fusion System/
├── run.bat                      ← 一键启动脚本
├── README.md                    ← 本文档
│
├── backend/                     ← 后端 + 数据生成
│   ├── app.py                   ← FastAPI 服务（数据/证据/对话/报告 API）
│   ├── nlu.py                   ← 自然语言理解引擎（意图识别+RAG+多轮对话+条件查询）
│   ├── generate_data.py         ← 多源勘察样例数据生成器
│   ├── __init__.py
│   └── data/                    ← 生成的数据（首次运行后出现）
│       ├── manifest.json        ← 总清单（线路、风险对象、数据源）
│       ├── dem/                 ← 数字高程模型 (PNG + world file)
│       ├── orthophoto/          ← 正射影像 (PNG)
│       ├── pointcloud/          ← 三维点云 (PLY, ~8万点)
│       ├── geophysics/          ← 物探剖面 (高密度电法 PNG+CSV)
│       ├── boreholes/           ← 钻孔柱状图 (PNG + 数据 JSON)
│       └── report/              ← 勘察报告 (JSON + TXT)
│
└── frontend/                    ← 前端
    ├── index.html               ← 主页面（三栏 + 底部轴布局）
    └── static/
        ├── app.js               ← 应用主逻辑（地图/3D/证据/问答/报告）
        ├── app.css              ← 深色科技主题样式
        ├── marked.min.js        ← Markdown 渲染（本地）
        └── lib/                 ← 三方库（均已本地下载）
            ├── leaflet/         ← 2D 地图
            ├── three/           ← 3D 渲染 (three + OrbitControls + PLYLoader)
            └── echarts/         ← 图表
```

---

## 🎬 演示流程（答辩推荐路径）

1. **打开平台** → 自动进入「XX 铁路隧道 K12+000~K13+000」场景
2. **左侧地图**呈现正射影像 + 线路 + 钻孔 + 物探线 + 3 个风险区
3. **点击「K12+380 洞口边坡失稳区」**（红色多边形）
4. 系统自动联动：
   - 🛰 **正射影像** → 冲沟发育、坡面裸露
   - 🏔 **三维点云** → 自动飞行到该区，显示 38° 坡度、52m 高差
   - 📡 **物探剖面** → H1-H1' 低阻异常（180Ω·m）
   - 🔩 **钻孔** → ZK3 揭示 0-8m 强风化碎裂花岗岩
   - 📄 **报告** → 6.3 节卸荷裂隙描述
5. **底部「风险解释」**显示综合判定 + 设计建议
6. 切到 **「智能对话」** → 用自然语言操作平台（见下方对话能力）
7. 切到 **「报告生成」** → 一键导出 Markdown 风险分析报告

---

## 💬 智能对话引擎（核心交互能力）

底部「智能对话」标签是一个**完全离线的自然语言交互引擎**（`backend/nlu.py`），无需任何外部大模型 API，比赛现场断网可用。

### 五类意图 + 自然语言操作

| 意图 | 示例指令 | 系统动作 |
|---|---|---|
| 🗺️ **定位** | "带我去看看 K12+380 的边坡" | 自动飞行定位 + 证据链联动 + 3D 聚焦 |
| 🔍 **查询** | "坡度大于 30 度的风险有哪些" / "水位浅于 3 米的钻孔" | 结构化条件过滤，返回符合条件的风险/钻孔 |
| 📊 **对比** | "R001 和 R002 哪个风险更高" | 生成对比表 + 结论 |
| 💡 **解释** | "K12+720 富水破碎带如何处理" | RAG 检索 + 综合解释 + 设计建议 |
| 📑 **报告** | "给 R001 生成一份报告" | 自动切到报告页并生成 |

### 关键特性

- **多轮上下文记忆**：连续对话保留状态。"带我去 K12+380" → "那它的截排水怎么设计"，"它"自动指代 R001
- **动作联动地图（Function Calling 本地实现）**：对话中识别出定位/报告意图时，自动触发地图飞行、证据链填充、报告生成
- **证据可追溯**：每个回答附带可点击的「证据来源」卡片，点击即跳转对应风险区
- **RAG 检索**：报告/风险/钻孔文本切成语料，TF-IDF 向量化（字符级 n-gram，对中文友好），召回相关片段

### 支持的查询语法

```
坡度大于 30 度的风险          → slope_gt=30
坡度小于 15 度的区域          → slope_lt=15
地下水位浅于 3 米的钻孔       → water_lt=3
电阻率小于 200 的风险         → rho_lt=200
K12+300 到 K12+800 之间的风险  → 里程范围查询
R001 和 R002 哪个风险更高      → 对比分析
```

---

## 📑 多格式报告生成引擎

底部「报告生成」标签可产出**正式工程交付文档**（`backend/report_gen.py`），完全本地生成，无需 Office/WPS。

### 三种格式 × 两种范围

| | 单风险报告 | 全线综合报告 |
|---|---|---|
| **Word (.docx)** | 封面 + 多源证据表 + 参数 + 解释 + 建议 + 钻孔地层表 + **嵌入物探/柱状图** | 工程概述 + 风险统计 + 逐风险分析 + 对比 + 总体结论 + **嵌入全部图件** |
| **Markdown (.md)** | 同上（纯文本） | 同上（纯文本） |
| **HTML (.html)** | 同上 + base64 内嵌图（浏览器可直接打印 PDF） | 同上 + 全部图件 |

### 使用方式

**界面操作**：底部「报告生成」标签 → 选范围（全线/单风险）→ 选格式（Word/MD/HTML）→ 点「生成预览」在线查看 → 点「下载文件」获取正式文档。

**对话触发**：在智能对话中输入「给 R001 生成一份报告」→ 自动切换到报告页并生成。

**API 调用**：
```
GET /api/report/preview?scope=full              # 全线报告预览
GET /api/report/download?scope=risk&rid=R001&fmt=docx   # 下载单风险 Word
```

### Word 报告结构（答辩可演示）

一份完整的全线综合 Word 报告包含：
- **封面**：项目名、报告类型、生成时间、生成方式
- **一、工程概述**：线路参数、钻孔/物探/风险统计表
- **二、风险统计**：全部风险一览表
- **三、逐风险多源证据分析**：每个风险含基本信息表、多源证据表、关键参数表、**嵌入物探剖面图**、**嵌入钻孔柱状图**、综合解释、设计建议、地层表
- **四、风险对比分析**：跨风险参数对比表 + 首要风险结论
- **五、总体结论与建议**：分优先级处置建议

> 实测：全线 Word 报告 243KB，含 15 个表格、7 张嵌入图片、67 个段落。

---

## 📊 多维证据可视化 (ECharts)

系统在证据链面板集成了 **四种** ECharts 可视化，把"数字"变成"图形证据"：

### 1. 风险雷达图（单风险）

选中任一风险区，证据面板顶部自动渲染 **6 维度雷达图**，把多源证据量化为 0-100 分：

| 维度 | 评分依据 |
|---|---|
| 地形坡度 | 最大坡度 / 45° × 100 |
| 高差起伏 | 相对高差 / 60m × 100 |
| 物探异常 | (1000 - 最低电阻率) / 900 × 100 |
| 钻孔揭露 | 风化层厚度 + (100-RQD) 加权 |
| 地下水 | (10 - 水位埋深) / 10 × 100 |
| 综合等级 | 高=90, 中高=70, 中=50 |

不同风险类型的雷达形状特征鲜明：**边坡风险**（R001）在坡度/高差维度突出，**富水风险**（R002）在物探/地下水维度突出 —— 一眼就能看出致险机理差异。

### 2. 多风险对比雷达（对话触发）

在智能对话中问「R001 和 R002 哪个风险更高」，系统自动在证据面板渲染**多风险叠加雷达图**，直观对比不同风险的多维特征。

### 3. 钻孔地层条形图

证据面板的钻孔卡片中，每个钻孔自动渲染 **ECharts 横向条形图**，按岩性着色展示地层分布，鼠标悬停显示层厚与描述 —— 比单纯看柱状图图片更易扫读。

### 4. 物探电阻率交互式热力图

证据面板的物探卡片中，系统从 CSV 原始数据（1000 个采样点：40 桩号 × 25 深度）实时渲染 **ECharts 热力图**：
- 低阻区（蓝紫色）与高阻区（红黄色）一目了然，**异常带自动标注红色虚线圈**
- 鼠标悬停任一网格显示精确电阻率值，低阻异常区还会弹出「⚠️ 低阻异常区」提示
- 可折叠展开原始 matplotlib 断面图作为对照

> 这是系统**唯一基于真实连续数据**（而非预标注）的可视化：电阻率网格由数据生成器按物理模型计算，热力图由前端实时渲染，体现"数据驱动"而非"图片展示"。

---

## 📐 沿线地质纵剖面（隧道工程核心图）

底部「📐 线路剖面」标签展示沿隧道线路（K12+000~K13+000）的**地质纵剖面图**——这是隧道工程地质勘察最重要的一张图，把地形、隧道、地层、风险统一在里程轴上。

### 图上内容

| 图层 | 说明 |
|---|---|
| 🏔️ **地表高程线**（绿色面积图） | 从 DEM 沿线路采样 21 个点，反映地形起伏（978~1038m） |
| ⛏️ **隧道设计高程线**（橙色虚线） | 简化纵坡 1.5%，反映隧道纵断面 |
| 📏 **隧道埋深** | 地表与隧道高程差，tooltip 实时显示（最深 44.7m，洞口段最浅 -4.5m） |
| 🔩 **钻孔投影**（青色三角） | 6 个钻孔按最近点投影到线路，标注孔号 |
| 🟥 **风险区段**（背景色块） | 3 个风险区按里程范围标注（红/橙/黄） |

### 工程价值

这张图回答了隧道设计的核心问题：**每个里程的埋深多少、哪里浅埋有风险、地层是什么、钻孔揭示什么**——是"线路里程"→"地形高程"→"地层分布"→"隧道埋深"的完整串联。

---

## 🌐 多元数据三维融合（核心亮点）

中间 3D 面板实现了**多元勘察数据的真正三维融合**——不是分屏对比，而是把 4 种数据源放在**同一个三维空间**里：

| 3D 图层 | 数据来源 | 渲染方式 |
|---|---|---|
| 🛰 **遥感影像地形** | 正射影像（DOM）+ DEM 高程 | PlaneGeometry（8000 顶点）贴正射影像纹理，受光照 |
| ☁️ **地表点云** | DEM 生成（8 万点） | Three.js Points，半透明叠加在地形上，坡度着色 |
| ⛏️ **隧道管体** | 沿线路中心线 + 设计纵坡 | TubeGeometry，橙色半透明管 + 线框 |
| 🔩 **钻孔分层柱** | 6 个钻孔地层 + 地下水位 | 分层圆柱体（按岩性着色）+ 蓝色水位盘 |
| ⚠️ **异常体** | 物探推断的不良地质体（富水/松动/松散） | 半透明椭球（蓝/红/黄） |

> **4 源融合**：遥感影像（地表纹理）+ 点云（坡度细节）+ 物探异常（地下不良体）+ 钻探地层（验证柱），在同一 3D 空间叠加展示，每层可独立开关。

### 融合的工程意义

| 看什么 | 回答什么问题 |
|---|---|
| 隧道穿过的地层 | 隧道与异常体的空间关系：隧道是否穿过富水破碎带？ |
| 钻孔与隧道 | 钻孔揭示的地层如何外推到隧道深度？ |
| 异常体与地表 | 地下低阻异常是否连通地表冲沟？ |

### 交互

- **左上角图层开关**：点云/影像地形/隧道/钻孔/异常体 独立显隐
- **左下角图例**：颜色含义说明
- **右上角工具**：定位风险区（3D 飞行）、重置视角、点大小
- 选中风险区 → 3D 自动飞行定位 + 白色边界框

### 空间感与坐标系（增强）

3D 场景新增多项空间参考元素，让观众一眼建立尺度与方位感：

| 元素 | 说明 |
|---|---|
| 🟦 **地面网格** | 1000×800m 区域，每 50m 一格细线，提供平面尺度参考 |
| ➤ **带标签三轴** | X(红,东/里程) Y(绿,北) Z(蓝,高程)，ArrowHelper + 中文标签 |
| 📏 **高程刻度尺** | Z 轴每 50m 一格（950~1100m），带数值标签 |
| 🧭 **指北针** | 场景东南角红色 N 箭头，明确方位 |
| ━ **比例尺** | 地表白色 100m 参考线段 |
| ⬚ **区域边界框** | 虚线勾勒 1000×800m 工程范围 |
| 🌫️ **雾效** | 远处淡入背景，增强空间深度层次 |
| ☀️ **多层光照** | 半球光+主光(太阳)+填充光，地形明暗立体感 |

---

## 🧪 内置风险案例

| ID | 里程 | 类型 | 等级 | 核心证据 |
|---|---|---|---|---|
| **R001** | K12+380 | 洞口边坡失稳 | 高 | 圈椅地貌 + 38°坡 + 低阻异常 + 强风化 |
| **R002** | K12+720 | 富水破碎带 | 中高 | 汇水洼地 + 深部低阻 + 地下水2.1m + RQD=22% |
| **R003** | K12+050 | 松散堆积 | 中 | 扇形堆积 + 缓坡 + 浅部松散层 6-9m |

---

## 🔌 API 一览

| 端点 | 方法 | 说明 |
|---|---|---|
| `/api/manifest` | GET | 项目总览（线路/风险/数据源清单） |
| `/api/risk/{id}` | GET | 风险对象详情 + 5 类证据卡片 |
| `/api/boreholes` | GET | 钻孔列表 |
| `/api/geophysics?lid=L1` | GET | 物探剖面 |
| `/api/geophysics/{lid}/grid` | GET | **物探电阻率网格数据**（40×25 网格，供热力图） |
| `/api/chat` | POST | **智能对话**（`{message, session_id}` → 意图+回答+动作+证据） |
| `/api/chat/suggest` | GET | 推荐快捷指令示例 |
| `/api/qa` | POST | 证据链问答（旧接口，`{question}`） |
| `/api/report` | GET | **报告能力总览**（格式/范围/风险清单） |
| `/api/report/preview` | GET | **报告在线预览**（`?scope=full\|risk&rid=R001` → Markdown+HTML） |
| `/api/report/download` | GET | **报告下载**（`?scope=full\|risk&rid=R001&fmt=docx\|md\|html`） |
| `/api/report/{id}` | GET | 单风险 Markdown 预览（旧兼容接口） |
| `/api/risk_scores` | GET | **风险多维评分**（`?rid=R001` 单个 / 全部，供雷达图） |
| `/api/profile/route` | GET | **沿线地质纵剖面**（地表高程+隧道+埋深+钻孔投影+风险区段） |
| `/api/3d/structures` | GET | **三维地质结构**（隧道轴线+钻孔分层+异常体，与点云同坐标系融合） |
| `/api/3d/terrain` | GET | **DEM 地形网格**（`?step=5` 降采样高程矩阵，供遥感纹理地形面） |
| `/api/search?q=` | GET | 关键词检索（简易 RAG 召回） |
| `/data/*` | GET | 静态数据文件（影像/点云/PNG/CSV） |

---

## 🛠 技术栈

- **前端**：原生 HTML/CSS/JS + Leaflet（2D 地图）+ Three.js（3D 点云）+ ECharts + marked.js
- **后端**：Python FastAPI + uvicorn
- **数据生成**：NumPy + matplotlib + Pillow（DEM/影像/点云/剖面/柱状图）
- **存储**：JSON / PNG / PLY / CSV 文件（轻量，无需数据库）

## 🎨 设计原则

1. **离线可用** —— 所有前端库本地打包，断网也能演示
2. **证据可追溯** —— 任何 AI 回答都附带可点击的证据来源
3. **轻量自洽** —— 数据由生成器一次性产出，逻辑闭环，不依赖外部服务
4. **演示友好** —— 一键启动，3 个典型案例覆盖核心风险类型

---

## ❓ 常见问题

**Q: 想重新生成数据？**
A: 删除 `backend/data` 文件夹，重新运行 `python backend/generate_data.py` 或 `run.bat`。

**Q: 想换一个工程案例？**
A: 修改 `backend/generate_data.py` 中的 `RISK_OBJECTS`、`BOREHOLES`、`GEOPHYSICS_LINES`、`SURVEY_REPORT` 后重新生成。

**Q: 端口被占用？**
A: 修改 `run.bat` 或启动命令中的 `--port 8000` 为其他端口。

**Q: 3D 点云看不到？**
A: 确认浏览器支持 WebGL（Chrome/Edge 均可）；点云首次加载约 2-3 秒。
